---
formatteroff: true
pagination:
  data: sides
  size: 24
permalink: 'sides/{% if pagination.pageNumber > 0 %}{{ pagination.pageNumber + 1 }}/{% endif %}index.html'
formatteron: true
---

{% set title = 'Projetos Pessoais' %}
{% set description = 'Listagem dos meus projetos pessoais.' %}
{% set keywords = ['projetos', 'pessoais', 'aprendizado', 'programação', 'desenvolvimento'] | join %}
{% set canonical = 'sides' | canonical %}

{% extends "layout.njk" %}
{% import 'macros.njk' as macro %}

{% block body %}
  <section class='container mt-2 mb-12 relative'>
    <div class='flex flex-col text-light'>
      <h1 class='font-ubuntu font-bold text-3xl leading-loose relative text-primary'>Projetos Pessoais</h1>
      <div
        class="flex flex-wrap justify-between lg:items-center w-full flex-col lg:flex-row"
      >
        <span>Projetos pessoais são projetos implementados com intuito de aprender alguma tecnologia.</span>
      </div>
    </div>
    <div class='card-grid'>
      {% for item in pagination.items %}
        {{ macro.card(item, 'side', '/sides/') }}
      {% endfor %}
    </div>
  </section>
  <nav class='container'>
    <ul class='list-none flex items-center justify-center text-light w-full' aria-labelledby='navigation'
        role='list'>
      <li aria-hidden='true' class='{{ 'invisible' if pagination.pageNumber == 0 }}'>
        <a href='{{ pagination.page.previous }}' target='_self'>
          <i class='feather feather-chevron-left'></i>
        </a>
      </li>
      {%- for item in pagination.hrefs %}
        <li class='ml-2'>
          <a href="{{ item }}"
             class='{{ 'text-primary' if pagination.pageNumber == loop.index0 }} hover:opacity-75 transition-colors duration-200 ease-in'
             target='_self'>
            {{ loop.index }}
          </a>
        </li>
      {% endfor -%}
      <li aria-hidden='true' class='{{ 'invisible' if pagination.pageNumber == pagination.hrefs.length - 1 }} ml-2'>
        <a href='{{ pagination.page.next }}' target='_self'>
          <i class='feather feather-chevron-right'></i>
        </a>
      </li>

    </ul>
  </nav>
{% endblock %}
